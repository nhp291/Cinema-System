import React from 'react';
import Layout from '../../containers/Layout_User';

const CinemaContact: React.FC = () => {
    return (
        <Layout>
            <div>
                <h1>Contact</h1>
            </div>
        </Layout>
    );
};

export default CinemaContact;